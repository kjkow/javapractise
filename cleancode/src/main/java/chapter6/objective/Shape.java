package chapter6.objective;

/**
 * Created by kamil on 2017-10-03.
 * First see description to class Geometry.
 * This is objective approach, its easier to add new classes without changing functions
 * its also harder to add new functions because one must change all the implementations
 */
public interface Shape {
    double area();
}
